class User {
  final String uid;
  User({this.uid});
}

class StudentOut {
  final String classR, id, name, phone, room, outTime, scannedBy;
  StudentOut(
      {this.classR,
      this.id,
      this.name,
      this.phone,
      this.room,
      this.outTime,
      this.scannedBy});
}
